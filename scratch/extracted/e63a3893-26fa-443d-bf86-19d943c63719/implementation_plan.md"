"# Configure Resend for Team Invitations\n\nIntegrate Resend to handle team invitation emails on the Creatabl platform. This includes schema changes to support invitation tokens and expiry, API modifications to send invitation emails, a route to handle validation/acceptance of invitations, and a beautifully designed client page for invitation acceptance.\n\n## User Review Required\n\n> [!IMPORTANT]\n> The implementation plan updates the database schema (`workspace_members` table) using Drizzle. We will run `npx drizzle-kit push --force` to push the new fields (`invitation_token`, `invitation_expires_at`). This is a safe addition since it only introduces optional columns (`text` and `timestamp`) without breaking existing queries.\n\n> [!NOTE]\n> Since we do not have the production Resend API Key, we will configure placeholder variables in `.env.local`:\n> - `RESEND_API_KEY=re_...`\n> - `RESEND_FROM_EMAIL=noreply@creatabl-ia.com`\n> You will need to replace `RESEND_API_KEY` with your actual Resend key.\n\n## Proposed Changes\n\n---\n\n### Database Schema\n\n#### [MODIFY] [schema.ts](file:///Users/naomiklock/Desktop/next.js/creatabl-app/creatabl-ia/lib/db/schema.ts)\nAdd `invitationToken` and `invitationExpiresAt` columns to the `workspaceMembers` table definition:\n- `invitationToken: text('invitation_token').unique()`\n- `invitationExpiresAt: timestamp('invitation_expires_at')`\n\n---\n\n### Email Service Integration\n\n#### [NEW] [email.ts](file:///Users/naomiklock/Desktop/next.js/creatabl-app/creatabl-ia/lib/email.ts)\nCreate a helper utility to send HTML emails using the `resend` client package. It constructs a responsive, brand-aligned email template matching the Creatabl branding (violet highlights, clean margins, responsive buttons).\n\n---\n\n### API Routes & Client Pages\n\n#### [MODIFY] [route.ts](file:///Users/naomiklock/Desktop/next.js/creatabl-app/creatabl-ia/app/api/team/members/route.ts)\nModify the POST handler to:\n1. Generate a secure 32-byte hexadecimal random token using Node's native `crypto` m
<truncated 1834 bytes>